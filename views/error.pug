extends tailwind.pug

block title
  title Error

block content
  div(class="max-w-2xl mx-auto px-4 py-12")
    div(class="bg-white rounded-2xl shadow-sm p-8 text-center")
      h1(class="text-3xl font-bold text-red-600 mb-4") Error
      p(class="text-gray-600 text-lg") #{message}
    a(href="/main_page" class="inline-block mt-6 text-sm text-gray-500 hover:text-gray-700") ← Back to main page
